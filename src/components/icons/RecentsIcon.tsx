import React from 'react';
import RecentsIconSvg from '../../../public/icons/recents-line.svg';

export const RecentsIcon = (props: React.SVGProps<SVGSVGElement>) => {
  return <RecentsIconSvg {...props} />;
};